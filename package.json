{
  "name": "seeking-chat-automation",
  "version": "1.0.0",
  "description": "Chrome extension for automating chat responses on Seeking.com",
  "main": "background.js",
  "scripts": {
    "test": "echo \"No tests configured yet\"",
    "build": "npm run clean && npm run copy",
    "clean": "rm -rf dist/",
    "copy": "mkdir -p dist && cp -r manifest.json *.js *.html *.css icons/ dist/",
    "zip": "npm run build && cd dist && zip -r ../seeking-chat-automation.zip *"
  },
  "keywords": [
    "chrome-extension",
    "automation",
    "chat",
    "seeking"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}